apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: details-api-deny-all-ingress
  namespace: mcp-demo
spec:
  podSelector:
    matchLabels:
      app: details-api
  policyTypes: [Ingress]
  ingress: []   # <- allow no ingress at all
